# Example environment configuration for BundesFAQ Chatbot
# Copy to .env and fill in real secrets.

# OpenAI / Azure OpenAI
OPENAI_API_KEY=your_openai_key_here
# Optional override for persistent Chroma vectorstore directory
VECTORSTORE_DIR=./backend/vectorstore
# Model selection (if switching away from default)
MODEL=gpt-3.5-turbo
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
